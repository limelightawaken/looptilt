# Local port publishing — use with:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up --build
services:
  postgres:
    ports:
      - '5432:5432'
  redis:
    ports:
      - '6379:6379'
  backend:
    ports:
      - '3001:3001'
  frontend:
    ports:
      - '3000:3000'
